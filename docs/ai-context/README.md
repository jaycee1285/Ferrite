# AI context archive (pre–v0.2.7)

This folder archives PRDs and task lists used for development through v0.2.6.x, so the repo can start fresh for **v0.2.7** with a new PRD and tasks.

## Contents

- **`prds/`** — Product requirement documents (current Taskmaster PRD snapshot + historical PRDs from `docs/ai-workflow/prds/`).
- **`tasks/`** — Task JSON snapshots (current `.taskmaster/tasks/tasks.json` as `tasks-v0.2.6.1.json` + historical task files from `docs/ai-workflow/tasks/`).

## Usage

- **Reference only** — Use these for context when drafting the new v0.2.7 PRD; do not edit as the source of truth.
- **Active work** — New PRD and tasks for v0.2.7 live in `.taskmaster/docs/` and `.taskmaster/tasks/` (Task Master).

Archived: 2026-02-07 (pre–0.2.7 prep).
